{
	"name": "jsline",
	"version": "0.0.1",
	"description": "JSline™ is an online JS interpreter for executing JS methods on the fly, filled with friendly dev features.",
	"main": "./out/main/index.js",
	"author": "verdu <verdu@live.com>",
	"homepage": "https://github.com/verdulife/dev-spot",
	"scripts": {
		"start": "electron-vite preview",
		"dev": "electron-vite dev",
		"build": "electron-vite build",
		"postinstall": "electron-builder install-app-deps",
		"build:win": "npm run build && electron-builder --win --config",
		"build:mac": "npm run build && electron-builder --mac --config",
		"build:linux": "npm run build && electron-builder --linux --config",
		"lint": "prettier --plugin-search-dir . --check . && eslint .",
		"format": "prettier --plugin-search-dir . --write ."
	},
	"dependencies": {
		"@electron-toolkit/preload": "^1.0.2",
		"@electron-toolkit/utils": "^1.0.2",
		"electron-updater": "^5.3.0"
	},
	"devDependencies": {
		"@babel/standalone": "^7.20.11",
		"@electron/notarize": "^1.2.3",
		"@sveltejs/vite-plugin-svelte": "^1.2.0",
		"@types/babel__standalone": "^7.1.4",
		"@types/postcss-preset-env": "^7.7.0",
		"copy": "^0.0.1",
		"electron": "^22.0.0",
		"electron-builder": "^22.10.3",
		"electron-store": "^8.1.0",
		"electron-vite": "^1.0.11",
		"eslint": "^8.28.0",
		"eslint-config-prettier": "^8.5.0",
		"eslint-plugin-svelte3": "^4.0.0",
		"js-interpreter": "^4.0.0",
		"postcss": "^8.4.20",
		"postcss-load-config": "^4.0.1",
		"postcss-preset-env": "^7.8.3",
		"prettier": "^2.8.0",
		"prettier-plugin-svelte": "^2.8.1",
		"svelte": "^3.53.1",
		"svelte-preprocess": "^5.0.0",
		"vite": "^3.2.4"
	}
}
